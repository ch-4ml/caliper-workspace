test:
  name: fixed-asset-benchmark
  description: A test benchmark
  workers:
    type: local
    number: 2
  rounds:
    - label: batch-limit-keys
      description: Round to limit the number of keys and generate assets using batch scheduling
      txDuration: 30
      rateControl:
        type: fixed-load
        opts:
          transactionLoad: 2
      workload:
        module: workload/batch-limit-keys.js
        arguments:
          contractId: fixed-asset
          byteSize: 8000
          batchSize: 10
          keyCount: 5
